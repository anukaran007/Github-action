---
  name: "Pull Request CI"
  on:
    pull_request:
      branches: 
          - "main"
  jobs:
    build:
      runs-on: "ubuntu-latest"
      steps:
        - uses: actions/checkout@v4
        - name: run maven package
          run : mvn package
  
          