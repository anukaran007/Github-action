---
  name: "Pull Request CI"
  on:
    push:
      branches: 
          - "main"
  jobs:
    build:
      runs-on: "self-hosted"
      steps:
        - uses: actions/checkout@v4
        - name: run maven package
          run : mvn package
  
          
